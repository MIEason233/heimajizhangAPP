# 黑马记账 APP — 项目文档

## 📌 项目概述

| 项目 | 内容 |
|------|------|
| **项目名称** | 黑马记账 |
| **项目类型** | 桌面记账应用 |
| **运行平台** | Windows 10/11 + macOS |
| **目标用户** | 个人用户，用于日常人民币花销记录 |
| **项目所有者** | 个人用户（不懂编程技术） |

---

## 🎯 核心功能

1. **记一笔**：记录每次支出，包含金额、日期、分类（2级）、备注
2. **花销列表**：按时间倒序查看所有支出记录，支持按分类筛选
3. **分类管理**：2级分类体系（一级大类 + 二级小类），预设分类 + 用户自定义，支持新增/修改/删除（预设分类受保护）
4. **统计概览**：按月查看各分类支出汇总，饼图 + 柱状图展示
5. **数据本地存储**：所有数据保存在用户电脑本地（SQLite数据库），不上传任何服务器
6. **贪吃蛇小游戏**：内置经典贪吃蛇，方向键操控，工作之余放松一下

---

## 🗂️ 分类体系

### 预设10大一级分类 + 40+二级小类

| 一级分类 | 图标 | 二级小类 |
|---------|:---:|---------|
| 餐饮饮食 | 🍜 | 早餐、午餐、晚餐、零食饮料、外卖、聚餐请客 |
| 交通出行 | 🚗 | 公交地铁、网约车/出租车、加油充电、停车费、火车飞机、共享单车 |
| 购物消费 | 🛒 | 衣服鞋帽、数码产品、日用品、美妆护肤、烟酒茶叶、其他购物 |
| 住房居家 | 🏠 | 房租/房贷、水电燃气、物业费、家具家电、维修保养、宽带通讯 |
| 娱乐休闲 | 🎮 | 电影演出、游戏充值、旅游度假、运动健身、宠物开销、其他娱乐 |
| 医疗健康 | 💊 | 看病买药、体检检查、保健品、健身卡 |
| 教育学习 | 📚 | 课程培训、图书资料、文具用品、考试报名 |
| 人情往来 | 🎁 | 红包礼物、婚礼生日、孝敬父母、慈善捐款 |
| 金融理财 | 💰 | 银行手续费、保险保费、投资亏损、贷款利息 |
| 其他支出 | 📦 | 快递物流、办公用品、其他杂项 |

---

## 🛠️ 技术栈（已确定）

| 技术 | 用途 | 选择原因 |
|------|------|---------|
| **Electron** | 桌面应用框架 | 跨平台（Win+Mac），生态成熟，能做出漂亮界面 |
| **React** | UI框架 | 组件化开发，社区最大，UI库丰富 |
| **TypeScript** | 编程语言 | 类型安全，减少低级错误 |
| **Ant Design** | UI组件库 | 中文生态好，表格/表单/日期选择器组件成熟 |
| **ECharts** | 图表库 | 百度出品，中文文档，饼图/柱状图效果好 |
| **better-sqlite3** | 本地数据库 | 数据存用户电脑，无需网络，启动快 |
| **Vite** | 构建工具 | 开发时热更新快，打包构建快 |
| **pnpm** | 包管理器 | 安装依赖快，磁盘占用少 |

### 为什么选 Electron？
- ✅ 界面可以做得非常美观（HTML+CSS的灵活性）
- ✅ 社区最成熟，遇到问题容易解决
- ✅ 大量现成组件可直接使用
- ❌ 安装包较大（80-150MB）
- ❌ 内存占用较多（150-300MB）

> 权衡：对于记账APP，界面美观和功能丰富 > 安装包体积，选 Electron 是最佳方案。

---

## 📁 项目结构

```
黑马记账APP/
├── CLAUDE.md                  # 本文件：项目文档与开发规则
├── package.json               # 项目配置与依赖
├── tsconfig.json              # TypeScript 配置
├── vite.config.ts             # Vite 构建配置
├── electron/                  # Electron 主进程（壳）
│   ├── main.ts               # 窗口创建、应用生命周期
│   └── preload.ts            # 安全桥接（暴露API给渲染进程）
├── src/                       # React 渲染进程（界面）
│   ├── main.tsx              # React 入口
│   ├── App.tsx               # 根组件（路由/布局）
│   ├── components/           # UI 组件
│   │   ├── AddRecord.tsx     # "记一笔"表单
│   │   ├── RecordList.tsx    # 花销列表
│   │   ├── Statistics.tsx    # 统计图表
│   │   ├── CategoryManager.tsx  # 分类管理
│   │   ├── SnakeGame.tsx     # 贪吃蛇小游戏
│   │   └── Layout.tsx        # 整体布局框架
│   ├── database/             # 数据库操作
│   │   ├── connection.ts     # 数据库连接
│   │   ├── categories.ts     # 分类数据操作
│   │   └── records.ts        # 记账记录操作
│   └── types/                # TypeScript 类型定义
│       └── index.ts          # 数据模型接口
└── resources/                 # 静态资源
    └── icon.png              # 应用图标
```

---

## 📊 数据模型

### categories 表（分类表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键，自增 |
| name | TEXT | 分类名称 |
| parent_id | INTEGER | 父分类ID（一级分类为 NULL） |
| icon | TEXT | 图标 emoji |
| sort_order | INTEGER | 排序序号 |
| is_preset | INTEGER | 是否为预设分类（预设不可删） |

### records 表（记账记录表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键，自增 |
| amount | REAL | 金额（元，支持小数） |
| date | TEXT | 日期（格式：YYYY-MM-DD） |
| category_id | INTEGER | 二级分类ID（外键→categories.id） |
| note | TEXT | 备注（可选） |
| created_at | TEXT | 创建时间 |

---

## ⚠️ 重要开发规则（必须遵守）

### 规则1：用户是编程小白
**用户明确声明不懂编程技术，无法提出具体技术要求。**

因此：
- ❌ **禁止**：直接问用户技术问题，如"用 Redux 还是 Zustand？"、"数据库用 MySQL 还是 SQLite？"
- ❌ **禁止**：使用技术术语而不解释
- ✅ **必须**：遇到技术决策时，提供 2-3 个方案，用通俗语言解释每个方案的优缺点
- ✅ **必须**：推荐一个方案并说明理由，让用户只需说"是"或"选A"
- ✅ **必须**：主动完成所有技术决策，不需要用户懂代码

### 规则2：方案呈现格式
当需要用户做选择时，使用以下格式：

```
## 需要你做一个选择：[用一句话说清楚要选什么]

### 方案A：[名称]（⭐推荐）
- 是什么：[用生活化比喻解释]
- 好处：[对用户有什么实际好处]
- 代价：[对用户有什么实际影响]

### 方案B：[名称]
- 是什么：[用生活化比喻解释]
- 好处：[对用户有什么实际好处]
- 代价：[对用户有什么实际影响]

### 我的推荐：方案A
因为 [用通俗语言解释理由]
```

### 规则3：解释一切
- 每次操作前，说明"我要做什么"和"为什么这么做"
- 每完成一个步骤，用完成状态汇报结果
- 如果出错，解释"出了什么问题"和"怎么解决"

### 规则4：文档先行
- 重大改动前先更新 CLAUDE.md
- CLAUDE.md 是项目的"唯一真相来源"

---

## 📝 开发约定

### 代码风格
- 所有注释使用中文
- 变量/函数名使用英文（遵循编程惯例）
- UI 文字使用中文

### 货币
- 本APP只处理人民币（¥ / CNY）
- 金额单位为"元"，支持两位小数
- 日期格式统一为 YYYY-MM-DD

### 数据安全
- 所有数据仅存储在用户本地电脑
- 不上传任何数据到服务器
- 不收集任何用户信息
- 无需网络连接即可使用全部功能

---

## 🚀 开发阶段

| 阶段 | 内容 | 状态 |
|------|------|:---:|
| 阶段1 | 初始化项目、搭建基础框架 | ✅ |
| 阶段2 | 实现"记一笔"功能 | ✅ |
| 阶段3 | 实现花销列表展示 | ✅ |
| 阶段4 | 实现分类管理与筛选（含自定义分类新增/修改/删除） | ✅ |
| 阶段5 | 实现统计概览（图表） | ✅ |
| 阶段6 | 添加贪吃蛇小游戏 | ✅ |
| 阶段7 | 打包发布（Win .exe + Mac .dmg） | ⬜ |

---

## ⚙️ 环境问题记录

### ELECTRON_RUN_AS_NODE 问题
- **问题**：VSCode 会在终端环境中设置 `ELECTRON_RUN_AS_NODE=1`，导致 Electron 以纯 Node.js 模式运行，`require('electron')` 返回二进制路径而非 API
- **解决**：已添加到 `~/.bashrc`：`unset ELECTRON_RUN_AS_NODE`
- **项目脚本**：所有 npm scripts 已添加 `unset ELECTRON_RUN_AS_NODE &&` 前缀

### better-sqlite3 编译
- better-sqlite3 是需要编译的原生模块，必须针对 Electron 内置的 Node.js 版本编译
- Electron 28 内置 Node.js v18.18.2（系统 Node.js 是 v24.14.0）
- 使用 `npx electron-rebuild -f -w better-sqlite3` 重新编译

### 文件夹命名冲突
- **问题**：项目中的 `electron/` 文件夹与 npm `electron` 包冲突
- **解决**：已重命名为 `main-process/`

---

> 📅 创建日期：2026-07-21
> 🔄 最后更新：2026-07-24
